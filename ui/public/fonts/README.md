# Fonts

Drop `AgaveNerdFont-Regular.woff2` here (from the Agave Nerd Font release) to
enable the terminal font. Without it the terminal falls back to the system
`ui-monospace` face. The main UI font (Roboto Mono Variable) ships via the
`@fontsource-variable/roboto-mono` package and needs no manual file.
